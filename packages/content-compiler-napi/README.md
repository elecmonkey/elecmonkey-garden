# @elecmonkey/garden-content-compiler

Rust-backed Markdown and content compiler for [Elecmonkey's Garden](https://www.elecmonkey.com/en).

This package exposes the Node.js API and Rsbuild plugin used by the site build. The native compiler is distributed through optional platform packages, so consumers can install the published package without a local Rust toolchain.
