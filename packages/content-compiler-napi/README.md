# @elecmonkey/garden-content-compiler

Node-facing package for the Garden content compiler. It will expose a napi binding backed by `crates/content-compiler` and provide the stable build-pipeline entry used by the site package.
