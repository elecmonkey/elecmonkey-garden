/* This file is generated by @elecmonkey/garden-content-compiler. Do not edit manually. */

import type { PostData } from '@/lib/api';

export const postSourceHash = "sha256:49b57c46efcbeab75703e875ce5954ce25d6051214e801de7600d32580493ebf";
export const postContentHash = "sha256:f1ab62ce3a1885c1ed3ab2fc91ca7719e3ec1e4b5faadd77a73fe44aabab9375";

export const post = {
  "id": "ssr",
  "content": "\n> This article was translated by AI and has not been manually reviewed.\n\n## Toward Client-Side Rendering\n\nServer-Side Rendering (SSR) is an application model in which the server renders a page into complete HTML and sends it directly to the client. This contrasts sharply with Client-Side Rendering (CSR), which usually sends only a minimal HTML skeleton and relies on JavaScript executed in the browser to render most of the content.\n\nWeb standards were originally born for documents. Later, to avoid notifying the server for every trivial matter, JavaScript gave clients programming capabilities. As requirements kept expanding, the JavaScript standard also continued to evolve, gradually gaining full network request and DOM manipulation capabilities.\n\nThis gave rise to an extremely imaginative approach: AJAX. Since all DOM nodes can be created by JavaScript on the client, a web page can contain only an HTML \"template,\" then fetch data through AJAX requests and dynamically fill the data into the DOM with JavaScript.\n\nAJAX's greatest contribution was bringing complete frontend-backend separation to the field of Web applications. From then on, frontend code no longer mixed with backend logic. The Web frontend evolved into a pure \"data consumer,\" interacting with the backend through unified APIs. jQuery pushed this AJAX model to its peak.\n\nThen modern frameworks such as React and Vue appeared. To reduce the pain of manually operating the DOM, they took over DOM manipulation comprehensively. To make it easier for frameworks to modify the DOM, it became simpler to let the framework create all DOM at the beginning. We only need to use a JavaScript data structure to represent the HTML template for the framework. Thus came JSX and Vue Single File Components.\n\nThis caused less and less HTML content to be sent directly to the browser. Eventually, the HTML of many pages was simplified to this:\n\n```html\n<html>\n  <body>\n    <div id=\"root\"></div>\n    <script type=\"module\" src=\"/src/index.ts\"></script>\n  </body>\n</html>\n```\n\nAll DOM nodes wait for JavaScript to be downloaded, parsed, and executed before they can be filled and rendered. This is what we now call **Client-Side Rendering (CSR)**. Obviously, CSR inevitably brings some small problems, such as **longer first-screen rendering time**, because the DOM only appears after JavaScript has finished loading; and worse SEO, although search engines can now partially crawl JavaScript-rendered page content.\n\nTo solve these problems without breaking the existing CSR system and the advantages it brings, an even more magical approach appeared: **Server-Side Rendering (SSR)**.\n\n## Detailed SSR Workflow\n\nWe can divide it into two main stages: **server processing** and **client hydration**. Server processing is implemented with Vite and Express, and client hydration is implemented with Vite.\n\n### 1. Receiving Requests and Preparing the Template\n\nWhen a user visits the website, the process begins:\n\n1.  **Server startup**: Create an Express application instance to listen for all incoming requests.\n2.  **Vite middleware**: Vite is integrated into Express in middleware mode (`middlewareMode: true`). It handles on-demand module loading, hot updates in development, and most importantly, code transformation.\n3.  **Read the HTML template**: For each request, the server first reads the contents of the `index.html` file. This file is a \"template\" with two placeholders:\n\n    ```html\n    <!-- index.html -->\n    <body>\n      <div id=\"app\"><!--ssr-outlet--></div>\n      <!--ssr-state-->\n      <script type=\"module\" src=\"/src/main.ts\"></script>\n    </body>\n    ```\n\n    -   `<!--ssr-outlet-->`: This location will be replaced by the dynamic application content rendered by the server.\n    -   `<!--ssr-state-->`: This location is used to inject the application's initial state so the client can take over. All initial \"state\" values, meaning some data, are represented as a JavaScript object and attached directly to the `window` object.\n\n### 2. Server-Side Rendering\n\nAfter obtaining the template, the server needs to generate dynamic content to fill the placeholders:\n\n1.  **Load the server entry module**: Vite's `vite.ssrLoadModule('/src/entry-server.ts')` executes the `entry-server.ts` file in the server environment. This is isolated from executing scripts in the browser.\n\n2.  **Generate application HTML**:\n    -   `entry-server.ts` calls the `render` function.\n    -   The `render` function receives an **initial state**, `{ count: 10 }` in this example, and calls the `createApp` function.\n    -   The `createApp` function in `app.ts` generates a pure HTML string for the application based on this initial state.\n\n    ```typescript\n    // src/app.ts\n    export function createApp(initialState: { count: number }) {\n      return `\n        <h1>Hello, SSR!</h1>\n        <p>This is a simple counter.</p>\n        <div id=\"counter\">Count: ${initialState.count}</div>\n        <button id=\"increment\">Increment</button>\n      `;\n    }\n    ```\n\n    At this point, we have obtained `appHtml`, whose content is an HTML fragment containing `Count: 10`.\n\n### 3. State Injection and HTML Assembly\n\nTo let the client-side JS know the initial state used when the application was rendered on the server, the server needs to pass this state down. The server serializes the initial state object `{ count: 10 }` into a string, embeds the serialized state into a `<script>` tag, and mounts it on the `window` object.\n\n```javascript\n// server.ts\nconst stateHtml = `<script>window.__INITIAL_STATE__ = {\"count\":10}</script>`;\n```\n\nThe server uses the `appHtml` and `stateHtml` generated in the previous step to replace the `<!--ssr-outlet-->` and `<!--ssr-state-->` placeholders in the `index.html` template respectively.\n\n```javascript\n// server.ts\nconst html = template\n    .replace(`<!--ssr-outlet-->`, appHtml)\n    .replace(`<!--ssr-state-->`, stateHtml);\n```\n\nFinally, the server sends the browser a complete HTML page containing all content.\n\n### 4. Hydration\n\nAfter receiving the complete HTML, the browser displays it immediately. But at this point the page is only static; interactions such as button clicks do not work. To make the page \"alive,\" client-side JavaScript must step in: **Hydration**.\n\n1.  **Execute the client script**: After parsing the HTML, the browser executes `<script type=\"module\" src=\"/src/main.ts\"></script>`. Obviously the browser cannot execute TypeScript code directly, so Vite handles the transformation here.\n2.  **Get the initial state**: The code in `main.ts` first reads the state injected by the server from `window.__INITIAL_STATE__`, so the client's initial state remains consistent with the server's.\n\n    ```typescript\n    // src/main.ts\n    let count = window.__INITIAL_STATE__.count; // count is 10\n    ```\n\n3.  **Bind event listeners**: The script obtains DOM elements, such as the button, and binds event listeners to them.\n\n    ```typescript\n    // src/main.ts\n    incrementButton.addEventListener('click', () => {\n        count++;\n        counterElement.textContent = `Count: ${count}`;\n    });\n    ```\n\nAt this point, the page has effectively been \"taken over\" by client-side JavaScript. It is now no different from CSR, and all later interactions can be handled by client-side JavaScript. Page navigation can be fake navigation, and form submissions can also use AJAX without refreshing the page.\n\n## Flowchart\n\n```mermaid\nsequenceDiagram\n    participant Client as 浏览器\n    participant Server as Express 服务器\n    participant Vite as Vite\n    participant Renderer as SSR 渲染器\n\n    Client->>Server: 发起页面请求 (GET /)\n    Server->>Vite: 传递请求\n    Vite->>Server: 读取 index.html 模板\n    Server->>Vite: 加载服务端模块<br/>vite.ssrLoadModule('/src/entry-server.ts')\n    Vite->>Renderer: 调用 render({ count: 10 })\n    Renderer-->>Vite: 返回应用 HTML ('<h1>...</h1>')\n    Vite-->>Server: 返回渲染后的 HTML 和初始状态\n    Server->>Server: 将应用 HTML 和状态脚本<br/>注入到模板中\n    Server-->>Client: 返回完整的 HTML 页面\n    Client->>Client: 立即渲染静态页面 (用户看到内容)\n    Client->>Client: 执行 main.ts 脚本\n    Client->>Client: 从 window.__INITIAL_STATE__ 读取状态\n    Client->>Client: 绑定事件监听器 (页面激活)\n    Note over Client: 页面现在完全可交互\n```\n",
  "html": "<blockquote>\n<p>This article was translated by AI and has not been manually reviewed.</p>\n</blockquote>\n<h2><a inert href=\"#toward-client-side-rendering\" aria-hidden=\"true\" class=\"anchor\" id=\"toward-client-side-rendering\"></a>Toward Client-Side Rendering</h2>\n<p>Server-Side Rendering (SSR) is an application model in which the server renders a page into complete HTML and sends it directly to the client. This contrasts sharply with Client-Side Rendering (CSR), which usually sends only a minimal HTML skeleton and relies on JavaScript executed in the browser to render most of the content.</p>\n<p>Web standards were originally born for documents. Later, to avoid notifying the server for every trivial matter, JavaScript gave clients programming capabilities. As requirements kept expanding, the JavaScript standard also continued to evolve, gradually gaining full network request and DOM manipulation capabilities.</p>\n<p>This gave rise to an extremely imaginative approach: AJAX. Since all DOM nodes can be created by JavaScript on the client, a web page can contain only an HTML &quot;template,&quot; then fetch data through AJAX requests and dynamically fill the data into the DOM with JavaScript.</p>\n<p>AJAX's greatest contribution was bringing complete frontend-backend separation to the field of Web applications. From then on, frontend code no longer mixed with backend logic. The Web frontend evolved into a pure &quot;data consumer,&quot; interacting with the backend through unified APIs. jQuery pushed this AJAX model to its peak.</p>\n<p>Then modern frameworks such as React and Vue appeared. To reduce the pain of manually operating the DOM, they took over DOM manipulation comprehensively. To make it easier for frameworks to modify the DOM, it became simpler to let the framework create all DOM at the beginning. We only need to use a JavaScript data structure to represent the HTML template for the framework. Thus came JSX and Vue Single File Components.</p>\n<p>This caused less and less HTML content to be sent directly to the browser. Eventually, the HTML of many pages was simplified to this:</p>\n<div data-md-island=\"code\" data-island-id=\"code-1\" data-language=\"html\"><pre><code class=\"language-html\">&lt;html&gt;\n  &lt;body&gt;\n    &lt;div id=&quot;root&quot;&gt;&lt;/div&gt;\n    &lt;script type=&quot;module&quot; src=&quot;/src/index.ts&quot;&gt;&lt;/script&gt;\n  &lt;/body&gt;\n&lt;/html&gt;\n</code></pre><button type=\"button\" data-copy-button>复制</button></div>\n<p>All DOM nodes wait for JavaScript to be downloaded, parsed, and executed before they can be filled and rendered. This is what we now call <strong>Client-Side Rendering (CSR)</strong>. Obviously, CSR inevitably brings some small problems, such as <strong>longer first-screen rendering time</strong>, because the DOM only appears after JavaScript has finished loading; and worse SEO, although search engines can now partially crawl JavaScript-rendered page content.</p>\n<p>To solve these problems without breaking the existing CSR system and the advantages it brings, an even more magical approach appeared: <strong>Server-Side Rendering (SSR)</strong>.</p>\n<h2><a inert href=\"#detailed-ssr-workflow\" aria-hidden=\"true\" class=\"anchor\" id=\"detailed-ssr-workflow\"></a>Detailed SSR Workflow</h2>\n<p>We can divide it into two main stages: <strong>server processing</strong> and <strong>client hydration</strong>. Server processing is implemented with Vite and Express, and client hydration is implemented with Vite.</p>\n<h3><a inert href=\"#1-receiving-requests-and-preparing-the-template\" aria-hidden=\"true\" class=\"anchor\" id=\"1-receiving-requests-and-preparing-the-template\"></a>1. Receiving Requests and Preparing the Template</h3>\n<p>When a user visits the website, the process begins:</p>\n<ol>\n<li>\n<p><strong>Server startup</strong>: Create an Express application instance to listen for all incoming requests.</p>\n</li>\n<li>\n<p><strong>Vite middleware</strong>: Vite is integrated into Express in middleware mode (<code>middlewareMode: true</code>). It handles on-demand module loading, hot updates in development, and most importantly, code transformation.</p>\n</li>\n<li>\n<p><strong>Read the HTML template</strong>: For each request, the server first reads the contents of the <code>index.html</code> file. This file is a &quot;template&quot; with two placeholders:</p>\n<div data-md-island=\"code\" data-island-id=\"code-2\" data-language=\"html\"><pre><code class=\"language-html\">&lt;!-- index.html --&gt;\n&lt;body&gt;\n  &lt;div id=&quot;app&quot;&gt;&lt;!--ssr-outlet--&gt;&lt;/div&gt;\n  &lt;!--ssr-state--&gt;\n  &lt;script type=&quot;module&quot; src=&quot;/src/main.ts&quot;&gt;&lt;/script&gt;\n&lt;/body&gt;\n</code></pre><button type=\"button\" data-copy-button>复制</button></div>\n<ul>\n<li><code>&lt;!--ssr-outlet--&gt;</code>: This location will be replaced by the dynamic application content rendered by the server.</li>\n<li><code>&lt;!--ssr-state--&gt;</code>: This location is used to inject the application's initial state so the client can take over. All initial &quot;state&quot; values, meaning some data, are represented as a JavaScript object and attached directly to the <code>window</code> object.</li>\n</ul>\n</li>\n</ol>\n<h3><a inert href=\"#2-server-side-rendering\" aria-hidden=\"true\" class=\"anchor\" id=\"2-server-side-rendering\"></a>2. Server-Side Rendering</h3>\n<p>After obtaining the template, the server needs to generate dynamic content to fill the placeholders:</p>\n<ol>\n<li>\n<p><strong>Load the server entry module</strong>: Vite's <code>vite.ssrLoadModule('/src/entry-server.ts')</code> executes the <code>entry-server.ts</code> file in the server environment. This is isolated from executing scripts in the browser.</p>\n</li>\n<li>\n<p><strong>Generate application HTML</strong>:</p>\n<ul>\n<li><code>entry-server.ts</code> calls the <code>render</code> function.</li>\n<li>The <code>render</code> function receives an <strong>initial state</strong>, <code>{ count: 10 }</code> in this example, and calls the <code>createApp</code> function.</li>\n<li>The <code>createApp</code> function in <code>app.ts</code> generates a pure HTML string for the application based on this initial state.</li>\n</ul>\n<div data-md-island=\"code\" data-island-id=\"code-3\" data-language=\"typescript\"><pre><code class=\"language-typescript\">// src/app.ts\nexport function createApp(initialState: { count: number }) {\n  return `\n    &lt;h1&gt;Hello, SSR!&lt;/h1&gt;\n    &lt;p&gt;This is a simple counter.&lt;/p&gt;\n    &lt;div id=&quot;counter&quot;&gt;Count: ${initialState.count}&lt;/div&gt;\n    &lt;button id=&quot;increment&quot;&gt;Increment&lt;/button&gt;\n  `;\n}\n</code></pre><button type=\"button\" data-copy-button>复制</button></div>\n<p>At this point, we have obtained <code>appHtml</code>, whose content is an HTML fragment containing <code>Count: 10</code>.</p>\n</li>\n</ol>\n<h3><a inert href=\"#3-state-injection-and-html-assembly\" aria-hidden=\"true\" class=\"anchor\" id=\"3-state-injection-and-html-assembly\"></a>3. State Injection and HTML Assembly</h3>\n<p>To let the client-side JS know the initial state used when the application was rendered on the server, the server needs to pass this state down. The server serializes the initial state object <code>{ count: 10 }</code> into a string, embeds the serialized state into a <code>&lt;script&gt;</code> tag, and mounts it on the <code>window</code> object.</p>\n<div data-md-island=\"code\" data-island-id=\"code-4\" data-language=\"javascript\"><pre><code class=\"language-javascript\">// server.ts\nconst stateHtml = `&lt;script&gt;window.__INITIAL_STATE__ = {&quot;count&quot;:10}&lt;/script&gt;`;\n</code></pre><button type=\"button\" data-copy-button>复制</button></div>\n<p>The server uses the <code>appHtml</code> and <code>stateHtml</code> generated in the previous step to replace the <code>&lt;!--ssr-outlet--&gt;</code> and <code>&lt;!--ssr-state--&gt;</code> placeholders in the <code>index.html</code> template respectively.</p>\n<div data-md-island=\"code\" data-island-id=\"code-5\" data-language=\"javascript\"><pre><code class=\"language-javascript\">// server.ts\nconst html = template\n    .replace(`&lt;!--ssr-outlet--&gt;`, appHtml)\n    .replace(`&lt;!--ssr-state--&gt;`, stateHtml);\n</code></pre><button type=\"button\" data-copy-button>复制</button></div>\n<p>Finally, the server sends the browser a complete HTML page containing all content.</p>\n<h3><a inert href=\"#4-hydration\" aria-hidden=\"true\" class=\"anchor\" id=\"4-hydration\"></a>4. Hydration</h3>\n<p>After receiving the complete HTML, the browser displays it immediately. But at this point the page is only static; interactions such as button clicks do not work. To make the page &quot;alive,&quot; client-side JavaScript must step in: <strong>Hydration</strong>.</p>\n<ol>\n<li>\n<p><strong>Execute the client script</strong>: After parsing the HTML, the browser executes <code>&lt;script type=&quot;module&quot; src=&quot;/src/main.ts&quot;&gt;&lt;/script&gt;</code>. Obviously the browser cannot execute TypeScript code directly, so Vite handles the transformation here.</p>\n</li>\n<li>\n<p><strong>Get the initial state</strong>: The code in <code>main.ts</code> first reads the state injected by the server from <code>window.__INITIAL_STATE__</code>, so the client's initial state remains consistent with the server's.</p>\n<div data-md-island=\"code\" data-island-id=\"code-6\" data-language=\"typescript\"><pre><code class=\"language-typescript\">// src/main.ts\nlet count = window.__INITIAL_STATE__.count; // count is 10\n</code></pre><button type=\"button\" data-copy-button>复制</button></div>\n</li>\n<li>\n<p><strong>Bind event listeners</strong>: The script obtains DOM elements, such as the button, and binds event listeners to them.</p>\n<div data-md-island=\"code\" data-island-id=\"code-7\" data-language=\"typescript\"><pre><code class=\"language-typescript\">// src/main.ts\nincrementButton.addEventListener('click', () =&gt; {\n    count++;\n    counterElement.textContent = `Count: ${count}`;\n});\n</code></pre><button type=\"button\" data-copy-button>复制</button></div>\n</li>\n</ol>\n<p>At this point, the page has effectively been &quot;taken over&quot; by client-side JavaScript. It is now no different from CSR, and all later interactions can be handled by client-side JavaScript. Page navigation can be fake navigation, and form submissions can also use AJAX without refreshing the page.</p>\n<h2><a inert href=\"#flowchart\" aria-hidden=\"true\" class=\"anchor\" id=\"flowchart\"></a>Flowchart</h2>\n<div data-md-island=\"mermaid\" data-island-id=\"mermaid-1\"><pre><code class=\"language-mermaid\">sequenceDiagram\n    participant Client as 浏览器\n    participant Server as Express 服务器\n    participant Vite as Vite\n    participant Renderer as SSR 渲染器\n\n    Client-&gt;&gt;Server: 发起页面请求 (GET /)\n    Server-&gt;&gt;Vite: 传递请求\n    Vite-&gt;&gt;Server: 读取 index.html 模板\n    Server-&gt;&gt;Vite: 加载服务端模块&lt;br/&gt;vite.ssrLoadModule('/src/entry-server.ts')\n    Vite-&gt;&gt;Renderer: 调用 render({ count: 10 })\n    Renderer--&gt;&gt;Vite: 返回应用 HTML ('&lt;h1&gt;...&lt;/h1&gt;')\n    Vite--&gt;&gt;Server: 返回渲染后的 HTML 和初始状态\n    Server-&gt;&gt;Server: 将应用 HTML 和状态脚本&lt;br/&gt;注入到模板中\n    Server--&gt;&gt;Client: 返回完整的 HTML 页面\n    Client-&gt;&gt;Client: 立即渲染静态页面 (用户看到内容)\n    Client-&gt;&gt;Client: 执行 main.ts 脚本\n    Client-&gt;&gt;Client: 从 window.__INITIAL_STATE__ 读取状态\n    Client-&gt;&gt;Client: 绑定事件监听器 (页面激活)\n    Note over Client: 页面现在完全可交互\n</code></pre></div>\n",
  "islands": [
    {
      "kind": "code",
      "id": "code-1",
      "language": "html"
    },
    {
      "kind": "code",
      "id": "code-2",
      "language": "html"
    },
    {
      "kind": "code",
      "id": "code-3",
      "language": "typescript"
    },
    {
      "kind": "code",
      "id": "code-4",
      "language": "javascript"
    },
    {
      "kind": "code",
      "id": "code-5",
      "language": "javascript"
    },
    {
      "kind": "code",
      "id": "code-6",
      "language": "typescript"
    },
    {
      "kind": "code",
      "id": "code-7",
      "language": "typescript"
    },
    {
      "kind": "mermaid",
      "id": "mermaid-1"
    }
  ],
  "title": "Implementing Server-Side Rendering with Vite",
  "date": "2025-07-16",
  "description": "An SSR demo, plus a bit of discussion about how the Web arrived at where it is today.",
  "tags": [
    "Server-Side Rendering (SSR)",
    "Web History",
    "Vite"
  ],
  "author": "Elecmonkey",
  "toc": [
    {
      "id": "toward-client-side-rendering",
      "text": "Toward Client-Side Rendering",
      "level": 2
    },
    {
      "id": "detailed-ssr-workflow",
      "text": "Detailed SSR Workflow",
      "level": 2
    },
    {
      "id": "1-receiving-requests-and-preparing-the-template",
      "text": "1. Receiving Requests and Preparing the Template",
      "level": 3
    },
    {
      "id": "2-server-side-rendering",
      "text": "2. Server-Side Rendering",
      "level": 3
    },
    {
      "id": "3-state-injection-and-html-assembly",
      "text": "3. State Injection and HTML Assembly",
      "level": 3
    },
    {
      "id": "4-hydration",
      "text": "4. Hydration",
      "level": 3
    },
    {
      "id": "flowchart",
      "text": "Flowchart",
      "level": 2
    }
  ],
  "locale": "en",
  "permalink": "/en/blog/ssr",
  "monthFolder": "202507",
  "isDraft": false,
  "isHidden": false,
  "prevPost": {
    "id": "rolldown-vite-production-ready",
    "title": "Is Rolldown-vite Worth Trying Now?"
  },
  "nextPost": {
    "id": "modern-frontend-soc",
    "title": "Technical Layering or Feature Cohesion: What Kind of Separation of Concerns Should Modern Frontend Have?"
  }
} satisfies PostData;
