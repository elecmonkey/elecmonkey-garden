/* This file is generated by @elecmonkey/garden-content-compiler. Do not edit manually. */

import type { PostData } from '@/lib/api';

export const postSourceHash = "sha256:9b244e1e520b7d6d4059eb19219bd353fa2faf5db4c3ca5adf968360bb15e5e9";
export const postContentHash = "sha256:4d4294c16f00b631711c93724645704ecc839abb9b450b3492746eb4b52dc112";

export const post = {
  "id": "use-react-native-and-expo",
  "content": "\n> This article was translated by AI and has not been manually reviewed.\n\nI have absolutely no mobile development experience or knowledge, and I have never touched Kotlin or Swift. This is purely a record of the process I eventually got working.\n\n## Creating the Project\n\n```bash\npnpx create-expo-app@latest\n```\n\nCreate a demo project, then let AI casually modify it a bit, rename it, and make a small demo that generates random numbers.\n\nAdd the following script to package.json:\n\n```json\n\"web\": \"expo start --web\"\n```\n\nRun:\n\n```bash\npnpm web\n```\n\nOpen the browser. If there are no major problems, use this demo for testing.\n\n\n## Android Side\n\n### Running on an Android Emulator\n\nInstall Android Studio! When Android Studio is opened for the first time, it will prompt you to install the Android SDK. I downloaded Android API 36, which seems to be the latest, though I am not very sure what range of Android versions it corresponds to.\n\nThen find Tools -> Device Manager at the top, create an Android phone emulator. I chose Pixel 7 Pro, API 36. Start the emulator and leave it running for later.\n\n`package.json` should have this by default:\n\n```json\n\"ios\": \"expo run:android\"\n```\n\nSo run:\n\n```bash\npnpm android\n```\n\nIf the emulator from earlier is running normally in the background, this step should connect to it. The first time requires waiting for Expo Go to be installed automatically, and there is a progress bar so you can watch it move. After it finishes:\n\n![rn-android-simulator](https://images.elecmonkey.com/articles/202601/rn-android-simulator.png)\n\n### Running on a Real Android Device\n\nExpo officially provides a hosted build service called EAS (Expo Application Services), which can directly build apk and ipa files (the latter requires a 99-dollar developer account). But Android is not that strict, so you can directly use EAS's free quota.\n\n\n```bash\n# Install the EAS command-line tool\npnpm add -g eas-cli\n# Log in to your Expo account\neas login\n```\n\nLog in to your Expo account,\n\n```bash\neas build:configure\n```\n\nIt will ask: Which platforms would you like to configure for EAS Build? Choose Android. This step will generate `eas.json`.\n\n```bash\neas build -p android --profile preview --clear-cache\n```\n\nFill in some signing-related information according to the prompts. The local cli may keep getting stuck (if the network is not very stable). At this point, open your own expo.dev dashboard, find this build task, and you can find the apk download link after the build succeeds. Download it and transfer it to the phone for installation.\n\n![rn-android-real](https://images.elecmonkey.com/articles/202601/rn-android-real.png)\n\n## iOS Side\n\n### Running on an iOS Simulator\n\nGo to the App Store and download a relatively new Xcode version. (For this, I was even forced to upgrade macOS first... It should now be the latest minor version of Sequoia. I really have some resistance to the Liquid Glass of macOS 26 Tahoe...)\n\nXcode includes iOS Simulator. You need to download a simulator image in advance. I tentatively downloaded iOS 26.\n\n![download-ios-26-simulator](https://images.elecmonkey.com/articles/202601/download-ios-26-simulator.png)\n\n`package.json` should have this by default:\n\n```json\n\"ios\": \"expo run:ios\"\n```\n\nSo run:\n\n```bash\npnpm ios\n```\n\nmacOS will automatically open iOS Simulator. The first launch requires waiting for Expo Go to be installed automatically. It also should not take too long, and there is a progress bar so you can watch it move. After it finishes:\n\n![rn-ios-simulator](https://images.elecmonkey.com/articles/202601/rn-ios-simulator.png)\n\n### Running on a Real iOS Device\n\nFirst of all, because I do not have a 99-dollar Apple Developer account, I cannot use the hosted service provided by Expo to build. I can only build locally with Xcode, connect a cable, and install it on my own iPhone in developer mode.\n\nFirst, on the Expo side, prebuild and package the JavaScript code,\n\nAdd two scripts to `package.json` yourself:\n\n```json\n\"prebuild\": \"expo prebuild\",\n\"export\": \"expo export\",\n```\n\nRun:\n\n```bash\npnpm prebuild\npnpm export\n```\n\n`prebuild` will generate the `ios` and `android` directories, containing native project code that can be opened with Xcode and Android Studio. `export` packages the JavaScript code for use by the native project.\n\nAt this point we need to install the relevant native dependencies for iOS development.\n\n```bash\nbrew install cocoapods\n```\n\nThen enter the `ios` directory and run:\n\n```bash\npod install\n```\n\nThe installation process seems to pull resources from GitHub, so pay attention to the network situation. After it completes successfully, `app.xcworkspace` will appear under the `ios` directory. This is what we need to open with Xcode. Use Finder to enter the `ios` directory and double-click `app.xcworkspace`.\n\nNow we are in Xcode!\n\nSet your developer identity information. In TARGETS -> app -> Signing & Capabilities, select your Apple ID account and change Team to your Apple ID (Personal Team). If this step is not set, you cannot build later. Bundle Identifier needs to be a unique identifier like com.yourname.appname, and it will automatically use the one from the React Native project.\n\nConnect the iPhone and choose the real device as the run target. The first time you connect, the phone needs to enable developer mode and follow the prompts to trust everything. I cannot remember the exact process clearly, but the system prompts are very clear, and I did not need extra help from search engines or AI. Some prompts are only in English.\n\nIn the navigation bar, click app under app ▸ iPhone, choose Edit Scheme, and change Debug to Release. If this step is not done, it seems the JS Bundle will not be packaged into the app.\n\n![release-app](https://images.elecmonkey.com/articles/202601/release-app.png)\n\nThen comes the moment to witness a miracle. Click the run button in the upper-left corner, and Xcode will start compiling, packaging, and installing to the real device. This process takes quite a while, so wait patiently.\n\n![rn-ios-real](https://images.elecmonkey.com/articles/202601/rn-ios-real.png)\n",
  "html": "<blockquote>\n<p>This article was translated by AI and has not been manually reviewed.</p>\n</blockquote>\n<p>I have absolutely no mobile development experience or knowledge, and I have never touched Kotlin or Swift. This is purely a record of the process I eventually got working.</p>\n<h2><a inert href=\"#creating-the-project\" aria-hidden=\"true\" class=\"anchor\" id=\"creating-the-project\"></a>Creating the Project</h2>\n<div data-md-island=\"code\" data-island-id=\"code-1\" data-language=\"bash\"><pre><code class=\"language-bash\">pnpx create-expo-app@latest\n</code></pre><button type=\"button\" data-copy-button>复制</button></div>\n<p>Create a demo project, then let AI casually modify it a bit, rename it, and make a small demo that generates random numbers.</p>\n<p>Add the following script to package.json:</p>\n<div data-md-island=\"code\" data-island-id=\"code-2\" data-language=\"json\"><pre><code class=\"language-json\">&quot;web&quot;: &quot;expo start --web&quot;\n</code></pre><button type=\"button\" data-copy-button>复制</button></div>\n<p>Run:</p>\n<div data-md-island=\"code\" data-island-id=\"code-3\" data-language=\"bash\"><pre><code class=\"language-bash\">pnpm web\n</code></pre><button type=\"button\" data-copy-button>复制</button></div>\n<p>Open the browser. If there are no major problems, use this demo for testing.</p>\n<h2><a inert href=\"#android-side\" aria-hidden=\"true\" class=\"anchor\" id=\"android-side\"></a>Android Side</h2>\n<h3><a inert href=\"#running-on-an-android-emulator\" aria-hidden=\"true\" class=\"anchor\" id=\"running-on-an-android-emulator\"></a>Running on an Android Emulator</h3>\n<p>Install Android Studio! When Android Studio is opened for the first time, it will prompt you to install the Android SDK. I downloaded Android API 36, which seems to be the latest, though I am not very sure what range of Android versions it corresponds to.</p>\n<p>Then find Tools -&gt; Device Manager at the top, create an Android phone emulator. I chose Pixel 7 Pro, API 36. Start the emulator and leave it running for later.</p>\n<p><code>package.json</code> should have this by default:</p>\n<div data-md-island=\"code\" data-island-id=\"code-4\" data-language=\"json\"><pre><code class=\"language-json\">&quot;ios&quot;: &quot;expo run:android&quot;\n</code></pre><button type=\"button\" data-copy-button>复制</button></div>\n<p>So run:</p>\n<div data-md-island=\"code\" data-island-id=\"code-5\" data-language=\"bash\"><pre><code class=\"language-bash\">pnpm android\n</code></pre><button type=\"button\" data-copy-button>复制</button></div>\n<p>If the emulator from earlier is running normally in the background, this step should connect to it. The first time requires waiting for Expo Go to be installed automatically, and there is a progress bar so you can watch it move. After it finishes:</p>\n<p><img src=\"https://images.elecmonkey.com/articles/202601/rn-android-simulator.png\" alt=\"rn-android-simulator\" /></p>\n<h3><a inert href=\"#running-on-a-real-android-device\" aria-hidden=\"true\" class=\"anchor\" id=\"running-on-a-real-android-device\"></a>Running on a Real Android Device</h3>\n<p>Expo officially provides a hosted build service called EAS (Expo Application Services), which can directly build apk and ipa files (the latter requires a 99-dollar developer account). But Android is not that strict, so you can directly use EAS's free quota.</p>\n<div data-md-island=\"code\" data-island-id=\"code-6\" data-language=\"bash\"><pre><code class=\"language-bash\"># Install the EAS command-line tool\npnpm add -g eas-cli\n# Log in to your Expo account\neas login\n</code></pre><button type=\"button\" data-copy-button>复制</button></div>\n<p>Log in to your Expo account,</p>\n<div data-md-island=\"code\" data-island-id=\"code-7\" data-language=\"bash\"><pre><code class=\"language-bash\">eas build:configure\n</code></pre><button type=\"button\" data-copy-button>复制</button></div>\n<p>It will ask: Which platforms would you like to configure for EAS Build? Choose Android. This step will generate <code>eas.json</code>.</p>\n<div data-md-island=\"code\" data-island-id=\"code-8\" data-language=\"bash\"><pre><code class=\"language-bash\">eas build -p android --profile preview --clear-cache\n</code></pre><button type=\"button\" data-copy-button>复制</button></div>\n<p>Fill in some signing-related information according to the prompts. The local cli may keep getting stuck (if the network is not very stable). At this point, open your own expo.dev dashboard, find this build task, and you can find the apk download link after the build succeeds. Download it and transfer it to the phone for installation.</p>\n<p><img src=\"https://images.elecmonkey.com/articles/202601/rn-android-real.png\" alt=\"rn-android-real\" /></p>\n<h2><a inert href=\"#ios-side\" aria-hidden=\"true\" class=\"anchor\" id=\"ios-side\"></a>iOS Side</h2>\n<h3><a inert href=\"#running-on-an-ios-simulator\" aria-hidden=\"true\" class=\"anchor\" id=\"running-on-an-ios-simulator\"></a>Running on an iOS Simulator</h3>\n<p>Go to the App Store and download a relatively new Xcode version. (For this, I was even forced to upgrade macOS first... It should now be the latest minor version of Sequoia. I really have some resistance to the Liquid Glass of macOS 26 Tahoe...)</p>\n<p>Xcode includes iOS Simulator. You need to download a simulator image in advance. I tentatively downloaded iOS 26.</p>\n<p><img src=\"https://images.elecmonkey.com/articles/202601/download-ios-26-simulator.png\" alt=\"download-ios-26-simulator\" /></p>\n<p><code>package.json</code> should have this by default:</p>\n<div data-md-island=\"code\" data-island-id=\"code-9\" data-language=\"json\"><pre><code class=\"language-json\">&quot;ios&quot;: &quot;expo run:ios&quot;\n</code></pre><button type=\"button\" data-copy-button>复制</button></div>\n<p>So run:</p>\n<div data-md-island=\"code\" data-island-id=\"code-10\" data-language=\"bash\"><pre><code class=\"language-bash\">pnpm ios\n</code></pre><button type=\"button\" data-copy-button>复制</button></div>\n<p>macOS will automatically open iOS Simulator. The first launch requires waiting for Expo Go to be installed automatically. It also should not take too long, and there is a progress bar so you can watch it move. After it finishes:</p>\n<p><img src=\"https://images.elecmonkey.com/articles/202601/rn-ios-simulator.png\" alt=\"rn-ios-simulator\" /></p>\n<h3><a inert href=\"#running-on-a-real-ios-device\" aria-hidden=\"true\" class=\"anchor\" id=\"running-on-a-real-ios-device\"></a>Running on a Real iOS Device</h3>\n<p>First of all, because I do not have a 99-dollar Apple Developer account, I cannot use the hosted service provided by Expo to build. I can only build locally with Xcode, connect a cable, and install it on my own iPhone in developer mode.</p>\n<p>First, on the Expo side, prebuild and package the JavaScript code,</p>\n<p>Add two scripts to <code>package.json</code> yourself:</p>\n<div data-md-island=\"code\" data-island-id=\"code-11\" data-language=\"json\"><pre><code class=\"language-json\">&quot;prebuild&quot;: &quot;expo prebuild&quot;,\n&quot;export&quot;: &quot;expo export&quot;,\n</code></pre><button type=\"button\" data-copy-button>复制</button></div>\n<p>Run:</p>\n<div data-md-island=\"code\" data-island-id=\"code-12\" data-language=\"bash\"><pre><code class=\"language-bash\">pnpm prebuild\npnpm export\n</code></pre><button type=\"button\" data-copy-button>复制</button></div>\n<p><code>prebuild</code> will generate the <code>ios</code> and <code>android</code> directories, containing native project code that can be opened with Xcode and Android Studio. <code>export</code> packages the JavaScript code for use by the native project.</p>\n<p>At this point we need to install the relevant native dependencies for iOS development.</p>\n<div data-md-island=\"code\" data-island-id=\"code-13\" data-language=\"bash\"><pre><code class=\"language-bash\">brew install cocoapods\n</code></pre><button type=\"button\" data-copy-button>复制</button></div>\n<p>Then enter the <code>ios</code> directory and run:</p>\n<div data-md-island=\"code\" data-island-id=\"code-14\" data-language=\"bash\"><pre><code class=\"language-bash\">pod install\n</code></pre><button type=\"button\" data-copy-button>复制</button></div>\n<p>The installation process seems to pull resources from GitHub, so pay attention to the network situation. After it completes successfully, <code>app.xcworkspace</code> will appear under the <code>ios</code> directory. This is what we need to open with Xcode. Use Finder to enter the <code>ios</code> directory and double-click <code>app.xcworkspace</code>.</p>\n<p>Now we are in Xcode!</p>\n<p>Set your developer identity information. In TARGETS -&gt; app -&gt; Signing &amp; Capabilities, select your Apple ID account and change Team to your Apple ID (Personal Team). If this step is not set, you cannot build later. Bundle Identifier needs to be a unique identifier like com.yourname.appname, and it will automatically use the one from the React Native project.</p>\n<p>Connect the iPhone and choose the real device as the run target. The first time you connect, the phone needs to enable developer mode and follow the prompts to trust everything. I cannot remember the exact process clearly, but the system prompts are very clear, and I did not need extra help from search engines or AI. Some prompts are only in English.</p>\n<p>In the navigation bar, click app under app ▸ iPhone, choose Edit Scheme, and change Debug to Release. If this step is not done, it seems the JS Bundle will not be packaged into the app.</p>\n<p><img src=\"https://images.elecmonkey.com/articles/202601/release-app.png\" alt=\"release-app\" /></p>\n<p>Then comes the moment to witness a miracle. Click the run button in the upper-left corner, and Xcode will start compiling, packaging, and installing to the real device. This process takes quite a while, so wait patiently.</p>\n<p><img src=\"https://images.elecmonkey.com/articles/202601/rn-ios-real.png\" alt=\"rn-ios-real\" /></p>\n",
  "islands": [
    {
      "kind": "code",
      "id": "code-1",
      "language": "bash"
    },
    {
      "kind": "code",
      "id": "code-2",
      "language": "json"
    },
    {
      "kind": "code",
      "id": "code-3",
      "language": "bash"
    },
    {
      "kind": "code",
      "id": "code-4",
      "language": "json"
    },
    {
      "kind": "code",
      "id": "code-5",
      "language": "bash"
    },
    {
      "kind": "code",
      "id": "code-6",
      "language": "bash"
    },
    {
      "kind": "code",
      "id": "code-7",
      "language": "bash"
    },
    {
      "kind": "code",
      "id": "code-8",
      "language": "bash"
    },
    {
      "kind": "code",
      "id": "code-9",
      "language": "json"
    },
    {
      "kind": "code",
      "id": "code-10",
      "language": "bash"
    },
    {
      "kind": "code",
      "id": "code-11",
      "language": "json"
    },
    {
      "kind": "code",
      "id": "code-12",
      "language": "bash"
    },
    {
      "kind": "code",
      "id": "code-13",
      "language": "bash"
    },
    {
      "kind": "code",
      "id": "code-14",
      "language": "bash"
    }
  ],
  "title": "Notes on Trying React Native Mobile Development with the Expo Framework",
  "date": "2026-01-26",
  "description": "A complete process record of getting started with React Native development using Expo, including Android and iOS simulators and real-device debugging",
  "tags": [
    "React",
    "React Native",
    "Expo",
    "Mobile",
    "iOS",
    "Android"
  ],
  "author": "Elecmonkey",
  "toc": [
    {
      "id": "creating-the-project",
      "text": "Creating the Project",
      "level": 2
    },
    {
      "id": "android-side",
      "text": "Android Side",
      "level": 2
    },
    {
      "id": "running-on-an-android-emulator",
      "text": "Running on an Android Emulator",
      "level": 3
    },
    {
      "id": "running-on-a-real-android-device",
      "text": "Running on a Real Android Device",
      "level": 3
    },
    {
      "id": "ios-side",
      "text": "iOS Side",
      "level": 2
    },
    {
      "id": "running-on-an-ios-simulator",
      "text": "Running on an iOS Simulator",
      "level": 3
    },
    {
      "id": "running-on-a-real-ios-device",
      "text": "Running on a Real iOS Device",
      "level": 3
    }
  ],
  "locale": "en",
  "permalink": "/en/blog/use-react-native-and-expo",
  "monthFolder": "202601",
  "isDraft": false,
  "isHidden": false,
  "prevPost": {
    "id": "frontend-framework-security-2025",
    "title": "Throwing Cold Water on Frontend Frameworks' Server-Side Ambitions: Two Critical React Ecosystem Vulnerabilities in 2025"
  },
  "nextPost": {
    "id": "about-astro",
    "title": "Writing at Astro v6 Beta: Astro Lets Real Websites Embrace the Modern Frontend Ecosystem"
  }
} satisfies PostData;
