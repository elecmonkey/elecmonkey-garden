/* This file is generated by @elecmonkey/garden-content-compiler. Do not edit manually. */

import type { PostData } from '@/lib/api';

export const postSourceHash = "sha256:b320dd5401e5a6ff7f283cd02a9b73cefb4cd73c245f97089dc932e8ae1ddf30";
export const postContentHash = "sha256:4633802a7750aa00a440197f8ead7e2bbbeddf57b45e48575e43ee1af7da65b3";

export const post = {
  "id": "javascript-reactive-systems",
  "content": "\n> This article was translated by AI and has not been manually reviewed.\n\n## \"Reactive Data\"\n\nIn traditional UI development, developers usually updated the UI in a \"command\" style: command a specific component to make a specific change. Whether in client development with Qt:\n\n```cpp\nQPushButton *button = new QPushButton(\"Click me\");\nconnect(button, &QPushButton::clicked, this, [this]() {\n    button->setText(\"Clicked!\");\n});\n```\n\nOr in Web development, operating the DOM directly:\n\n```js\nconst button = document.querySelector(\"button\");\nbutton.addEventListener(\"click\", () => {\n    button.textContent = \"Clicked!\";\n});\n```\n\nAt that time, the tool popular across frontend development was jQuery. Using it could reduce a lot of repetitive DOM operations:\n\n```js\n$(\"button\").click(function() {\n    $(this).text(\"Clicked!\");\n});\n```\n\nWhen the interface is simple, the code is still clear at a glance. But as interface data grows and update logic becomes more complex, the code develops an obvious characteristic: **the logic for updating the UI and the logic of the data itself are coupled together**. Yet UI and data are actually different things. The same set of data logic may be rendered on different UI platforms. For example, I may want to implement a Web application, then also build a desktop client with Qt, perhaps write a mini program, and then implement an Android app with Kotlin. Their data logic is the same and is **UI-independent**.\n\nSo the frontend world produced all kinds of UI frameworks, responsible for taking over UI update logic, while we developers only need to care about the data logic itself: the business logic we actually need to implement. This separation made many things possible. For example, by following the same API, React's underlying layer can also render on mobile devices, which is why we have React Native.\n\nBesides wrapping large amounts of DOM operations, frameworks all moved toward a \"declarative\" style, using declarations to describe what the UI should \"look like\". As for how the data is rendered into the DOM, frameworks take full control. A concept called \"reactive data\" was proposed. It allows us to declaratively describe dependencies between data. When data changes, all computations and UI that depend on that data automatically update. This pattern not only makes code clearer, but also makes state management more predictable.\n\nOver the past decade, frontend frameworks have gone through multiple iterations in reactive-system implementation. The core idea of the reactive systems we are familiar with today first gained attention in the open source JavaScript Web framework [Knockout.js](https://knockoutjs.com/) in 2010, and was later popularized by [Angular](https://angular.dev/). In the following years, many variants and implementations were created, from Vue 2's `Object.defineProperty` to Vue 3's `Proxy`, and from SolidJS's fine-grained reactivity. Every framework has explored more efficient and easier-to-use reactive solutions. Recently, the TC39 committee proposed the Signals proposal, attempting to provide standardized reactive primitives for JavaScript. This may completely change how frontend frameworks implement reactivity.\n\n## Mainstream Framework Implementations\n\n### Vue 2\n\nVue 2's reactive system is implemented based on `Object.defineProperty`. This is an API provided by the JavaScript standard. It allows us to intercept access and modification operations on object properties.\n\n```js\nconst data = {\n  count: 0\n}\n\nObject.defineProperty(data, 'count', {\n  get: () => data.count,\n  set: (value) => data.count = value\n})\n```\n\nA so-called reactive system means that when one of my values changes, values related to it will automatically recalculate, like an Excel spreadsheet: $A2 = A1 * 2$, and when A1 changes, A2 automatically recalculates. Related \"actions\" will also automatically re-execute.\n\nIn fact, \"updating values related to it (**reactive data**)\" (**computed properties**) is also a kind of related \"action\" (**side effect**). Our core goal is to \"automatically collect (**dependency collection**) and execute (**dispatch updates**) these actions\".\n\nThe core of Vue 2's reactive system consists of two mechanisms: **dependency collection** and **dispatching updates**:\n\nWhen a component renders, it accesses reactive data and triggers the getter. Vue **collects** the currently rendering component as a dependency in the getter.\n\n```js\n// 依赖收集示例\n\nlet activeEffect = null;\nfunction effect(fn) {\n  activeEffect = fn;\n  // 执行 effect，这会触发响应式数据的 getter\n  fn();\n  activeEffect = null;\n}\n\nfunction defineReactive(obj, key, val) {\n  const deps = new Set(); // 存储依赖的桶🪣\n\n  Object.defineProperty(obj, key, {\n    get() {\n      // 收集依赖，进桶吧您嘞\n      // 这就是为什么副作用一定要用effect包起来——Vue才能拿到这个函数\n      deps.add(activeEffect);\n      return val;\n    },\n    set(newVal) {\n      if (val === newVal) return;\n      val = newVal;\n      // 派发更新：挨个儿调用\n      deps.forEach(effect => effect());\n    }\n  });\n}\n\n// 使用示例，下面的写法写过Vue太熟悉了\nconst data = { count: 0 };\ndefineReactive(data, 'count', 0);\n\n// 创建一个 effect，它会自动收集依赖\neffect(() => {\n  console.log('count is:', data.count);\n});\n\n// 修改数据，触发更新\ndata.count = 1; // 输出: count is: 1\n```\n\nWhen reactive data changes, it triggers the setter. Vue **dispatches updates** and notifies all components that depend on the data to re-render.\n\n```js\n// 简化的组件更新机制\nfunction createComponent(options) {\n  const component = {\n    render() {\n      // 组件渲染函数\n      activeEffect = () => {\n        // 重新渲染组件\n        component.update();\n      };\n      // 执行渲染，触发依赖收集\n      return options.render.call(this);\n    },\n    update() {\n      // 更新 DOM\n    }\n  };\n  return component;\n}\n```\n\nVue 2's actual implementation is much more complex, but the idea is roughly this. In Vue 2, the following usage is common:\n\n```vue\n<template>\n  <div>\n    <p>Count: {{ count }}</p>\n    <p>Doubled: {{ doubled }}</p>\n    <button @click=\"increment\">Increment</button>\n  </div>\n</template>\n\n<script>\nexport default {\n  data() {\n    return {\n      count: 0\n    }\n  },\n  computed: {\n    doubled() {\n      return this.count * 2\n    }\n  },\n  methods: {\n    increment() {\n      this.count++\n    }\n  }\n}\n</script>\n```\n\n### Vue 3 with Composition API\n\nThe `Object.defineProperty` used by Vue 2 has the following limitations:\n\n- It cannot detect adding or deleting object properties\n- It cannot detect direct modification of array indexes or changes to array length\n- It requires recursively traversing all properties of an object to make deeply nested objects reactive\n\nVue 3's reactive system is implemented based on `Proxy`. The `Proxy` API was introduced in ES6. It provides more powerful object interception capabilities and can intercept almost all object operations, solving many limitations of `Object.defineProperty`, such as inability to observe added properties, deleted properties, and array index changes.\n\n```js\nconst data = {\n  count: 0\n}\n\nconst proxy = new Proxy(data, {\n  get: (target, prop) => target[prop],\n  set: (target, prop, value) => target[prop] = value\n})\n```\n\nVue 3 introduced the Composition API and Setup syntax sugar, making it more elegant to aggregate \"logic that belongs to the same part\" together.\n\n```vue\n<template>\n  <div>\n    <p>Count: {{ count }}</p>\n    <p>Doubled: {{ doubled }}</p>\n    <button @click=\"increment\">Increment</button>\n  </div>\n</template>\n\n<script setup>\nimport { ref, computed } from 'vue'\n\nconst count = ref(0)\nconst doubled = computed(() => count.value * 2)\n\nfunction increment() {\n  count.value++\n}\n</script>\n```\n\n### SolidJS & Signals\n\nSolid says: our core process is to collect the functions that use this data (dependency collection), and when the data changes, execute those functions (dispatch updates). In fact, `Proxy` and `Object.defineProperty` are just two APIs that wrap this for us, allowing developers to write code in a style close to native JS variables. They are not required. We can implement this idea directly without any wrapping, building a bare-metal-style reactive system. Whether you read or write, you have to call functions provided by my framework. Since I provide those functions, inserting my own logic inside them to do dependency collection and dispatch updates is easy.\n\nAt the same time, Solid also uses JSX, giving it a React-flavored style:\n\n```jsx\nimport { createSignal, createEffect } from 'solid-js'\n\nfunction Counter() {\n  const [count, setCount] = createSignal(0)\n  const doubled = () => count() * 2\n\n  createEffect(() => {\n    console.log(`Count: ${count()}, Doubled: ${doubled()}`)\n  })\n\n  return (\n    <div>\n      <p>Count: {count()}</p>\n      <p>Doubled: {doubled()}</p>\n      <button onClick={() => setCount(c => c + 1)}>Increment</button>\n    </div>\n  )\n}\n```\n\n## Core Concepts of Reactive Systems\n\nAt this point, we can roughly abstract the core concepts of various reactive systems. We need a **Signal**: a **readable and writable** value. When it changes, **Computed** values change and **side effects** are automatically triggered.\n\n### Signal\n\n```typescript\ntype Signal<T> = [() => T, (value: T) => void];\n\nfunction createSignal<T>(value: T): Signal<T> {\n  const subscribers = new Set<Effect>();\n\n  function read(): T {\n    if (currentEffect) {\n      subscribers.add(currentEffect);\n    }\n    return value;\n  }\n\n  function write(newValue: T): void {\n    if (value === newValue) return;\n    value = newValue;\n    const effectsToRun = new Set(subscribers);\n    effectsToRun.forEach(effect => {\n      try {\n        effect();\n      } catch (e) {\n        console.error('Error running effect:', e);\n      }\n    });\n  }\n\n  return [read, write];\n}\n```\n\nSignal is the foundation of a reactive system. It contains:\n\n- A value\n- A set of subscribers\n- A read method that automatically collects dependencies\n- A write method that notifies all subscribers\n\n### Effect\n\n```typescript\ntype Effect = () => void;\nlet currentEffect: Effect | null = null;\n\nfunction effect(fn: Effect): void {\n  const wrappedEffect = () => {\n    currentEffect = wrappedEffect;\n    try {\n      fn();\n    } finally {\n      currentEffect = null;\n    }\n  };\n  wrappedEffect();\n}\n```\n\nEffect is the execution unit of a reactive system:\n\n- It runs once when created\n- It re-runs when its dependent signals change\n- It tracks the side effect currently being \"collected\" through a global variable\n\n### Computed\n\n```typescript\nfunction computed<T>(fn: () => T): () => T {\n  const [get, set] = createSignal<T>(fn());\n\n  effect(() => {\n    const newValue = fn();\n    set(newValue);\n  });\n\n  return get;\n}\n```\n\nComputed is a value derived from other signals:\n\n- It calculates the initial value when created\n- It automatically recalculates when dependent signals change\n\n## TC39 Signals Proposal\n\nNow the question comes. So many frameworks have manually implemented similar functionality, which means this is a highly demanded feature. `Proxy` entering the ES standard greatly simplified Vue 3's implementation and enhanced its capabilities. Signal is a very good thing. If we let it enter the ES standard, framework designers only need to wrap the API.\n\nThe [TC39 Signals](https://github.com/tc39/proposal-signals) proposal aims to provide standardized signal primitives for JavaScript. It is currently at TC39 proposal Stage 1.\n\nThe current proposal includes three core concepts:\n\n1. `Signal.State` - writable state signal\n2. `Signal.Computed` - derived computed signal\n3. `Signal.Watcher` - low-level API for observing signal changes\n\nThe proposal presents a bright future:\n\n```js\n// 创建一个可写的状态信号，初始值为0\nconst counter = new Signal.State(0);\n\n// 创建一个计算信号，依赖于counter\nconst isEven = new Signal.Computed(() => (counter.get() & 1) == 0);\n\n// 创建另一个计算信号，依赖于isEven\nconst parity = new Signal.Computed(() => isEven.get() ? \"even\" : \"odd\");\n\n// 库或框架可以基于Signal原语定义effect\ndeclare function effect(cb: () => void): (() => void);\n\n// 创建一个effect，当parity变化时更新DOM\neffect(() => element.innerText = parity.get());\n\n// 模拟外部更新counter...\n// 每秒counter加1，这会触发一系列更新：\n// 1. counter变化\n// 2. isEven重新计算\n// 3. parity重新计算\n// 4. effect重新执行，更新DOM\nsetInterval(() => counter.set(counter.get() + 1), 1000);\n```\n\nThe proposal currently already has a polyfill implementation: [signal-polyfill](https://github.com/proposal-signals/signal-polyfill).\n",
  "html": "<blockquote>\n<p>This article was translated by AI and has not been manually reviewed.</p>\n</blockquote>\n<h2><a inert href=\"#reactive-data\" aria-hidden=\"true\" class=\"anchor\" id=\"reactive-data\"></a>&quot;Reactive Data&quot;</h2>\n<p>In traditional UI development, developers usually updated the UI in a &quot;command&quot; style: command a specific component to make a specific change. Whether in client development with Qt:</p>\n<div data-md-island=\"code\" data-island-id=\"code-1\" data-language=\"cpp\"><pre><code class=\"language-cpp\">QPushButton *button = new QPushButton(&quot;Click me&quot;);\nconnect(button, &amp;QPushButton::clicked, this, [this]() {\n    button-&gt;setText(&quot;Clicked!&quot;);\n});\n</code></pre><button type=\"button\" data-copy-button>复制</button></div>\n<p>Or in Web development, operating the DOM directly:</p>\n<div data-md-island=\"code\" data-island-id=\"code-2\" data-language=\"js\"><pre><code class=\"language-js\">const button = document.querySelector(&quot;button&quot;);\nbutton.addEventListener(&quot;click&quot;, () =&gt; {\n    button.textContent = &quot;Clicked!&quot;;\n});\n</code></pre><button type=\"button\" data-copy-button>复制</button></div>\n<p>At that time, the tool popular across frontend development was jQuery. Using it could reduce a lot of repetitive DOM operations:</p>\n<div data-md-island=\"code\" data-island-id=\"code-3\" data-language=\"js\"><pre><code class=\"language-js\">$(&quot;button&quot;).click(function() {\n    $(this).text(&quot;Clicked!&quot;);\n});\n</code></pre><button type=\"button\" data-copy-button>复制</button></div>\n<p>When the interface is simple, the code is still clear at a glance. But as interface data grows and update logic becomes more complex, the code develops an obvious characteristic: <strong>the logic for updating the UI and the logic of the data itself are coupled together</strong>. Yet UI and data are actually different things. The same set of data logic may be rendered on different UI platforms. For example, I may want to implement a Web application, then also build a desktop client with Qt, perhaps write a mini program, and then implement an Android app with Kotlin. Their data logic is the same and is <strong>UI-independent</strong>.</p>\n<p>So the frontend world produced all kinds of UI frameworks, responsible for taking over UI update logic, while we developers only need to care about the data logic itself: the business logic we actually need to implement. This separation made many things possible. For example, by following the same API, React's underlying layer can also render on mobile devices, which is why we have React Native.</p>\n<p>Besides wrapping large amounts of DOM operations, frameworks all moved toward a &quot;declarative&quot; style, using declarations to describe what the UI should &quot;look like&quot;. As for how the data is rendered into the DOM, frameworks take full control. A concept called &quot;reactive data&quot; was proposed. It allows us to declaratively describe dependencies between data. When data changes, all computations and UI that depend on that data automatically update. This pattern not only makes code clearer, but also makes state management more predictable.</p>\n<p>Over the past decade, frontend frameworks have gone through multiple iterations in reactive-system implementation. The core idea of the reactive systems we are familiar with today first gained attention in the open source JavaScript Web framework <a href=\"https://knockoutjs.com/\">Knockout.js</a> in 2010, and was later popularized by <a href=\"https://angular.dev/\">Angular</a>. In the following years, many variants and implementations were created, from Vue 2's <code>Object.defineProperty</code> to Vue 3's <code>Proxy</code>, and from SolidJS's fine-grained reactivity. Every framework has explored more efficient and easier-to-use reactive solutions. Recently, the TC39 committee proposed the Signals proposal, attempting to provide standardized reactive primitives for JavaScript. This may completely change how frontend frameworks implement reactivity.</p>\n<h2><a inert href=\"#mainstream-framework-implementations\" aria-hidden=\"true\" class=\"anchor\" id=\"mainstream-framework-implementations\"></a>Mainstream Framework Implementations</h2>\n<h3><a inert href=\"#vue-2\" aria-hidden=\"true\" class=\"anchor\" id=\"vue-2\"></a>Vue 2</h3>\n<p>Vue 2's reactive system is implemented based on <code>Object.defineProperty</code>. This is an API provided by the JavaScript standard. It allows us to intercept access and modification operations on object properties.</p>\n<div data-md-island=\"code\" data-island-id=\"code-4\" data-language=\"js\"><pre><code class=\"language-js\">const data = {\n  count: 0\n}\n\nObject.defineProperty(data, 'count', {\n  get: () =&gt; data.count,\n  set: (value) =&gt; data.count = value\n})\n</code></pre><button type=\"button\" data-copy-button>复制</button></div>\n<p>A so-called reactive system means that when one of my values changes, values related to it will automatically recalculate, like an Excel spreadsheet: <span class=\"katex\"><span class=\"katex-mathml\"><math xmlns=\"http://www.w3.org/1998/Math/MathML\"><semantics><mrow><mi>A</mi><mn>2</mn><mo>=</mo><mi>A</mi><mn>1</mn><mo>∗</mo><mn>2</mn></mrow><annotation encoding=\"application/x-tex\">A2 = A1 * 2</annotation></semantics></math></span><span class=\"katex-html\" aria-hidden=\"true\"><span class=\"base\"><span class=\"strut\" style=\"height:0.6833em;\"></span><span class=\"mord mathnormal\">A</span><span class=\"mord\">2</span><span class=\"mspace\" style=\"margin-right:0.2778em;\"></span><span class=\"mrel\">=</span><span class=\"mspace\" style=\"margin-right:0.2778em;\"></span></span><span class=\"base\"><span class=\"strut\" style=\"height:0.6833em;\"></span><span class=\"mord mathnormal\">A</span><span class=\"mord\">1</span><span class=\"mspace\" style=\"margin-right:0.2222em;\"></span><span class=\"mbin\">∗</span><span class=\"mspace\" style=\"margin-right:0.2222em;\"></span></span><span class=\"base\"><span class=\"strut\" style=\"height:0.6444em;\"></span><span class=\"mord\">2</span></span></span></span>, and when A1 changes, A2 automatically recalculates. Related &quot;actions&quot; will also automatically re-execute.</p>\n<p>In fact, &quot;updating values related to it (<strong>reactive data</strong>)&quot; (<strong>computed properties</strong>) is also a kind of related &quot;action&quot; (<strong>side effect</strong>). Our core goal is to &quot;automatically collect (<strong>dependency collection</strong>) and execute (<strong>dispatch updates</strong>) these actions&quot;.</p>\n<p>The core of Vue 2's reactive system consists of two mechanisms: <strong>dependency collection</strong> and <strong>dispatching updates</strong>:</p>\n<p>When a component renders, it accesses reactive data and triggers the getter. Vue <strong>collects</strong> the currently rendering component as a dependency in the getter.</p>\n<div data-md-island=\"code\" data-island-id=\"code-5\" data-language=\"js\"><pre><code class=\"language-js\">// 依赖收集示例\n\nlet activeEffect = null;\nfunction effect(fn) {\n  activeEffect = fn;\n  // 执行 effect，这会触发响应式数据的 getter\n  fn();\n  activeEffect = null;\n}\n\nfunction defineReactive(obj, key, val) {\n  const deps = new Set(); // 存储依赖的桶🪣\n\n  Object.defineProperty(obj, key, {\n    get() {\n      // 收集依赖，进桶吧您嘞\n      // 这就是为什么副作用一定要用effect包起来——Vue才能拿到这个函数\n      deps.add(activeEffect);\n      return val;\n    },\n    set(newVal) {\n      if (val === newVal) return;\n      val = newVal;\n      // 派发更新：挨个儿调用\n      deps.forEach(effect =&gt; effect());\n    }\n  });\n}\n\n// 使用示例，下面的写法写过Vue太熟悉了\nconst data = { count: 0 };\ndefineReactive(data, 'count', 0);\n\n// 创建一个 effect，它会自动收集依赖\neffect(() =&gt; {\n  console.log('count is:', data.count);\n});\n\n// 修改数据，触发更新\ndata.count = 1; // 输出: count is: 1\n</code></pre><button type=\"button\" data-copy-button>复制</button></div>\n<p>When reactive data changes, it triggers the setter. Vue <strong>dispatches updates</strong> and notifies all components that depend on the data to re-render.</p>\n<div data-md-island=\"code\" data-island-id=\"code-6\" data-language=\"js\"><pre><code class=\"language-js\">// 简化的组件更新机制\nfunction createComponent(options) {\n  const component = {\n    render() {\n      // 组件渲染函数\n      activeEffect = () =&gt; {\n        // 重新渲染组件\n        component.update();\n      };\n      // 执行渲染，触发依赖收集\n      return options.render.call(this);\n    },\n    update() {\n      // 更新 DOM\n    }\n  };\n  return component;\n}\n</code></pre><button type=\"button\" data-copy-button>复制</button></div>\n<p>Vue 2's actual implementation is much more complex, but the idea is roughly this. In Vue 2, the following usage is common:</p>\n<div data-md-island=\"code\" data-island-id=\"code-7\" data-language=\"vue\"><pre><code class=\"language-vue\">&lt;template&gt;\n  &lt;div&gt;\n    &lt;p&gt;Count: {{ count }}&lt;/p&gt;\n    &lt;p&gt;Doubled: {{ doubled }}&lt;/p&gt;\n    &lt;button @click=&quot;increment&quot;&gt;Increment&lt;/button&gt;\n  &lt;/div&gt;\n&lt;/template&gt;\n\n&lt;script&gt;\nexport default {\n  data() {\n    return {\n      count: 0\n    }\n  },\n  computed: {\n    doubled() {\n      return this.count * 2\n    }\n  },\n  methods: {\n    increment() {\n      this.count++\n    }\n  }\n}\n&lt;/script&gt;\n</code></pre><button type=\"button\" data-copy-button>复制</button></div>\n<h3><a inert href=\"#vue-3-with-composition-api\" aria-hidden=\"true\" class=\"anchor\" id=\"vue-3-with-composition-api\"></a>Vue 3 with Composition API</h3>\n<p>The <code>Object.defineProperty</code> used by Vue 2 has the following limitations:</p>\n<ul>\n<li>It cannot detect adding or deleting object properties</li>\n<li>It cannot detect direct modification of array indexes or changes to array length</li>\n<li>It requires recursively traversing all properties of an object to make deeply nested objects reactive</li>\n</ul>\n<p>Vue 3's reactive system is implemented based on <code>Proxy</code>. The <code>Proxy</code> API was introduced in ES6. It provides more powerful object interception capabilities and can intercept almost all object operations, solving many limitations of <code>Object.defineProperty</code>, such as inability to observe added properties, deleted properties, and array index changes.</p>\n<div data-md-island=\"code\" data-island-id=\"code-8\" data-language=\"js\"><pre><code class=\"language-js\">const data = {\n  count: 0\n}\n\nconst proxy = new Proxy(data, {\n  get: (target, prop) =&gt; target[prop],\n  set: (target, prop, value) =&gt; target[prop] = value\n})\n</code></pre><button type=\"button\" data-copy-button>复制</button></div>\n<p>Vue 3 introduced the Composition API and Setup syntax sugar, making it more elegant to aggregate &quot;logic that belongs to the same part&quot; together.</p>\n<div data-md-island=\"code\" data-island-id=\"code-9\" data-language=\"vue\"><pre><code class=\"language-vue\">&lt;template&gt;\n  &lt;div&gt;\n    &lt;p&gt;Count: {{ count }}&lt;/p&gt;\n    &lt;p&gt;Doubled: {{ doubled }}&lt;/p&gt;\n    &lt;button @click=&quot;increment&quot;&gt;Increment&lt;/button&gt;\n  &lt;/div&gt;\n&lt;/template&gt;\n\n&lt;script setup&gt;\nimport { ref, computed } from 'vue'\n\nconst count = ref(0)\nconst doubled = computed(() =&gt; count.value * 2)\n\nfunction increment() {\n  count.value++\n}\n&lt;/script&gt;\n</code></pre><button type=\"button\" data-copy-button>复制</button></div>\n<h3><a inert href=\"#solidjs-signals\" aria-hidden=\"true\" class=\"anchor\" id=\"solidjs-signals\"></a>SolidJS &amp; Signals</h3>\n<p>Solid says: our core process is to collect the functions that use this data (dependency collection), and when the data changes, execute those functions (dispatch updates). In fact, <code>Proxy</code> and <code>Object.defineProperty</code> are just two APIs that wrap this for us, allowing developers to write code in a style close to native JS variables. They are not required. We can implement this idea directly without any wrapping, building a bare-metal-style reactive system. Whether you read or write, you have to call functions provided by my framework. Since I provide those functions, inserting my own logic inside them to do dependency collection and dispatch updates is easy.</p>\n<p>At the same time, Solid also uses JSX, giving it a React-flavored style:</p>\n<div data-md-island=\"code\" data-island-id=\"code-10\" data-language=\"jsx\"><pre><code class=\"language-jsx\">import { createSignal, createEffect } from 'solid-js'\n\nfunction Counter() {\n  const [count, setCount] = createSignal(0)\n  const doubled = () =&gt; count() * 2\n\n  createEffect(() =&gt; {\n    console.log(`Count: ${count()}, Doubled: ${doubled()}`)\n  })\n\n  return (\n    &lt;div&gt;\n      &lt;p&gt;Count: {count()}&lt;/p&gt;\n      &lt;p&gt;Doubled: {doubled()}&lt;/p&gt;\n      &lt;button onClick={() =&gt; setCount(c =&gt; c + 1)}&gt;Increment&lt;/button&gt;\n    &lt;/div&gt;\n  )\n}\n</code></pre><button type=\"button\" data-copy-button>复制</button></div>\n<h2><a inert href=\"#core-concepts-of-reactive-systems\" aria-hidden=\"true\" class=\"anchor\" id=\"core-concepts-of-reactive-systems\"></a>Core Concepts of Reactive Systems</h2>\n<p>At this point, we can roughly abstract the core concepts of various reactive systems. We need a <strong>Signal</strong>: a <strong>readable and writable</strong> value. When it changes, <strong>Computed</strong> values change and <strong>side effects</strong> are automatically triggered.</p>\n<h3><a inert href=\"#signal\" aria-hidden=\"true\" class=\"anchor\" id=\"signal\"></a>Signal</h3>\n<div data-md-island=\"code\" data-island-id=\"code-11\" data-language=\"typescript\"><pre><code class=\"language-typescript\">type Signal&lt;T&gt; = [() =&gt; T, (value: T) =&gt; void];\n\nfunction createSignal&lt;T&gt;(value: T): Signal&lt;T&gt; {\n  const subscribers = new Set&lt;Effect&gt;();\n\n  function read(): T {\n    if (currentEffect) {\n      subscribers.add(currentEffect);\n    }\n    return value;\n  }\n\n  function write(newValue: T): void {\n    if (value === newValue) return;\n    value = newValue;\n    const effectsToRun = new Set(subscribers);\n    effectsToRun.forEach(effect =&gt; {\n      try {\n        effect();\n      } catch (e) {\n        console.error('Error running effect:', e);\n      }\n    });\n  }\n\n  return [read, write];\n}\n</code></pre><button type=\"button\" data-copy-button>复制</button></div>\n<p>Signal is the foundation of a reactive system. It contains:</p>\n<ul>\n<li>A value</li>\n<li>A set of subscribers</li>\n<li>A read method that automatically collects dependencies</li>\n<li>A write method that notifies all subscribers</li>\n</ul>\n<h3><a inert href=\"#effect\" aria-hidden=\"true\" class=\"anchor\" id=\"effect\"></a>Effect</h3>\n<div data-md-island=\"code\" data-island-id=\"code-12\" data-language=\"typescript\"><pre><code class=\"language-typescript\">type Effect = () =&gt; void;\nlet currentEffect: Effect | null = null;\n\nfunction effect(fn: Effect): void {\n  const wrappedEffect = () =&gt; {\n    currentEffect = wrappedEffect;\n    try {\n      fn();\n    } finally {\n      currentEffect = null;\n    }\n  };\n  wrappedEffect();\n}\n</code></pre><button type=\"button\" data-copy-button>复制</button></div>\n<p>Effect is the execution unit of a reactive system:</p>\n<ul>\n<li>It runs once when created</li>\n<li>It re-runs when its dependent signals change</li>\n<li>It tracks the side effect currently being &quot;collected&quot; through a global variable</li>\n</ul>\n<h3><a inert href=\"#computed\" aria-hidden=\"true\" class=\"anchor\" id=\"computed\"></a>Computed</h3>\n<div data-md-island=\"code\" data-island-id=\"code-13\" data-language=\"typescript\"><pre><code class=\"language-typescript\">function computed&lt;T&gt;(fn: () =&gt; T): () =&gt; T {\n  const [get, set] = createSignal&lt;T&gt;(fn());\n\n  effect(() =&gt; {\n    const newValue = fn();\n    set(newValue);\n  });\n\n  return get;\n}\n</code></pre><button type=\"button\" data-copy-button>复制</button></div>\n<p>Computed is a value derived from other signals:</p>\n<ul>\n<li>It calculates the initial value when created</li>\n<li>It automatically recalculates when dependent signals change</li>\n</ul>\n<h2><a inert href=\"#tc39-signals-proposal\" aria-hidden=\"true\" class=\"anchor\" id=\"tc39-signals-proposal\"></a>TC39 Signals Proposal</h2>\n<p>Now the question comes. So many frameworks have manually implemented similar functionality, which means this is a highly demanded feature. <code>Proxy</code> entering the ES standard greatly simplified Vue 3's implementation and enhanced its capabilities. Signal is a very good thing. If we let it enter the ES standard, framework designers only need to wrap the API.</p>\n<p>The <a href=\"https://github.com/tc39/proposal-signals\">TC39 Signals</a> proposal aims to provide standardized signal primitives for JavaScript. It is currently at TC39 proposal Stage 1.</p>\n<p>The current proposal includes three core concepts:</p>\n<ol>\n<li><code>Signal.State</code> - writable state signal</li>\n<li><code>Signal.Computed</code> - derived computed signal</li>\n<li><code>Signal.Watcher</code> - low-level API for observing signal changes</li>\n</ol>\n<p>The proposal presents a bright future:</p>\n<div data-md-island=\"code\" data-island-id=\"code-14\" data-language=\"js\"><pre><code class=\"language-js\">// 创建一个可写的状态信号，初始值为0\nconst counter = new Signal.State(0);\n\n// 创建一个计算信号，依赖于counter\nconst isEven = new Signal.Computed(() =&gt; (counter.get() &amp; 1) == 0);\n\n// 创建另一个计算信号，依赖于isEven\nconst parity = new Signal.Computed(() =&gt; isEven.get() ? &quot;even&quot; : &quot;odd&quot;);\n\n// 库或框架可以基于Signal原语定义effect\ndeclare function effect(cb: () =&gt; void): (() =&gt; void);\n\n// 创建一个effect，当parity变化时更新DOM\neffect(() =&gt; element.innerText = parity.get());\n\n// 模拟外部更新counter...\n// 每秒counter加1，这会触发一系列更新：\n// 1. counter变化\n// 2. isEven重新计算\n// 3. parity重新计算\n// 4. effect重新执行，更新DOM\nsetInterval(() =&gt; counter.set(counter.get() + 1), 1000);\n</code></pre><button type=\"button\" data-copy-button>复制</button></div>\n<p>The proposal currently already has a polyfill implementation: <a href=\"https://github.com/proposal-signals/signal-polyfill\">signal-polyfill</a>.</p>\n",
  "islands": [
    {
      "kind": "code",
      "id": "code-1",
      "language": "cpp"
    },
    {
      "kind": "code",
      "id": "code-2",
      "language": "js"
    },
    {
      "kind": "code",
      "id": "code-3",
      "language": "js"
    },
    {
      "kind": "code",
      "id": "code-4",
      "language": "js"
    },
    {
      "kind": "code",
      "id": "code-5",
      "language": "js"
    },
    {
      "kind": "code",
      "id": "code-6",
      "language": "js"
    },
    {
      "kind": "code",
      "id": "code-7",
      "language": "vue"
    },
    {
      "kind": "code",
      "id": "code-8",
      "language": "js"
    },
    {
      "kind": "code",
      "id": "code-9",
      "language": "vue"
    },
    {
      "kind": "code",
      "id": "code-10",
      "language": "jsx"
    },
    {
      "kind": "code",
      "id": "code-11",
      "language": "typescript"
    },
    {
      "kind": "code",
      "id": "code-12",
      "language": "typescript"
    },
    {
      "kind": "code",
      "id": "code-13",
      "language": "typescript"
    },
    {
      "kind": "code",
      "id": "code-14",
      "language": "js"
    }
  ],
  "title": "JavaScript Reactive Systems, from Framework Implementations to TC39 Signals",
  "date": "2025-05-07",
  "description": "In the fragmented frontend ecosystem, reactive data has become a faintly emerging new consensus",
  "tags": [
    "Reactive Systems",
    "Vue",
    "Solid.js",
    "JavaScript",
    "ECMAScript"
  ],
  "author": "Elecmonkey",
  "toc": [
    {
      "id": "reactive-data",
      "text": "\"Reactive Data\"",
      "level": 2
    },
    {
      "id": "mainstream-framework-implementations",
      "text": "Mainstream Framework Implementations",
      "level": 2
    },
    {
      "id": "vue-2",
      "text": "Vue 2",
      "level": 3
    },
    {
      "id": "vue-3-with-composition-api",
      "text": "Vue 3 with Composition API",
      "level": 3
    },
    {
      "id": "solidjs-signals",
      "text": "SolidJS & Signals",
      "level": 3
    },
    {
      "id": "core-concepts-of-reactive-systems",
      "text": "Core Concepts of Reactive Systems",
      "level": 2
    },
    {
      "id": "signal",
      "text": "Signal",
      "level": 3
    },
    {
      "id": "effect",
      "text": "Effect",
      "level": 3
    },
    {
      "id": "computed",
      "text": "Computed",
      "level": 3
    },
    {
      "id": "tc39-signals-proposal",
      "text": "TC39 Signals Proposal",
      "level": 2
    }
  ],
  "locale": "en",
  "permalink": "/en/blog/javascript-reactive-systems",
  "monthFolder": "202505",
  "isDraft": false,
  "isHidden": false,
  "prevPost": {
    "id": "mermaid-local-render",
    "title": "How to Force Mermaid, Born for the Browser, to Render on the Server"
  },
  "nextPost": {
    "id": "tech-debt",
    "title": "Running an Antique Frontend Project on an arm64 Mac, and Ending Up with a Head Full of Thoughts About Technical Debt"
  }
} satisfies PostData;
