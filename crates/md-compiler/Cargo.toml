[package]
name = "garden-md-compiler"
version.workspace = true
edition.workspace = true
publish = false

[lib]
name = "garden_md_compiler"
path = "src/lib.rs"

[dependencies]
comrak = { version = "0.52", default-features = false }
katex_rs = { package = "katex-rs", version = "0.2.4" }
